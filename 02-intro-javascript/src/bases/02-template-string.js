const name = 'Name';
const surname = 'Surname';

const completeName = `${ name } ${surname}`;

console.log(completeName);

function getSalute() {
    return 'Hello World';
}

console.log(`This is a text: ${getSalute()}`);